batch_size: 128
num_samples: 5000

patience: 5

latent_features: 32

learning_rate: 0.001
num_epochs: 100
lambda_rec: 1.0
lambda_kl: 0.001 # ưu tiên reconstruction loss hơn

lambda_ssim: 0.84

dataset: "CIFAR10"
in_channels: 3